defaults:
  - default.yaml

sft_config:
  num_train_epochs: 100
  #TODO test
  max_steps: 2

dataset: datasets/health/model=xz97-AlpaCare-llama2-13b_t=0.7_size=60000/public_seed.parquet
tags: [sft, health]
adapters_paths: ???
